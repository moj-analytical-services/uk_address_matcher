name: Deploy MkDocs (GitHub Pages)

on:
    pull_request:
        branches: [main]
        paths:
            - mkdocs.yml
            - docs/**
            - package.json
            - package-lock.json
            - vite.config.js
            - uk_address_matcher/labelling/app/**
    push:
        branches: [main]
        paths:
            - mkdocs.yml
            - docs/**
            - package.json
            - package-lock.json
            - vite.config.js
            - uk_address_matcher/labelling/app/**

permissions:
    contents: read
    pages: write
    id-token: write

concurrency:
    group: pages
    cancel-in-progress: true

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout
              uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
              with:
                  fetch-depth: 0

            - name: Set up Pages
              uses: actions/configure-pages@45bfe0192ca1faeb007ade9deae92b16b8254a0d # v6.0.0

            - name: Set up Node
              uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
              with:
                  node-version: "22"

            - name: Install frontend dependencies
              run: npm ci

            - name: Build labelling app
              run: npm run build

            - name: Install uv
              uses: astral-sh/setup-uv@cec208311dfd045dd5311c1add060b2062131d57 # v8.0.0
              with:
                  version: "0.11.3"

            - name: Set up Python
              uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
              with:
                  python-version: "3.12"

            - name: Install docs dependencies
              run: uv sync --group docs

            - name: Build site
              run: uv run mkdocs build --strict

            - name: Add labelling app to site
              run: |
                  mkdir -p site/labelling-tool
                  cp -R uk_address_matcher/labelling/app/static/. site/labelling-tool/

            - name: Upload Pages artifact
              uses: actions/upload-pages-artifact@7b1f4a764d45c48632c6b24a0339c27f5614fb0b # v4.0.0
              with:
                  path: site

    deploy:
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        runs-on: ubuntu-latest
        needs: build
        environment:
            name: github-pages
            url: ${{ steps.deployment.outputs.page_url }}
        steps:
            - name: Deploy to GitHub Pages
              id: deployment
              uses: actions/deploy-pages@cd2ce8fcbc39b97be8ca5fce6e763baed58fa128 # v5.0.0
